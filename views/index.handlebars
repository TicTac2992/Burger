<h1>Eat-Da-Burger!</h1>

<h2>What do you want a bite of?</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger_name devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers You've Already Eaten...</h2>

<ul>
  {{#each burgers}}
    {{#if newBurger}}
      {{> burgers/burger_name devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add ANY Burger You Want!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burgerName">Burger Name:</label>
    <input type="text" id="burgerName" name="name">
  </div>

  <div class="form-group">
    <label for="devour">Devour It!</label><br>
    <input type="radio" name="burger" value="1" checked> Devour It!<br>
    <input type="radio" name="devour" value="0"> Eaten Burgers...
  </div>

  <button type="submit">Add a burger!</button>
</form>
